@model IEnumerable<gender.Model.Document>
@{
    var subject = (gender.Model.Subject)ViewBag.Subject;
}
@if (Model.Any())
{
    <h3 class="section-header"><i class="icon-file"></i>&nbsp;Документы</h3>
    <div class="white-card">
        <ul class="icons-gree">
            @foreach (var item in Model)
            {
                <li>
                    <i class="icon-file"></i>&nbsp;<a href="@Url.Action("Item", "Document", new { url = item.Url })">@item.Header</a>
                </li>
            }
        </ul>
        <a href="@Url.Action("Item", "Subject", new { path = subject.FullUrl })#document">Еще</a>
    </div>
}